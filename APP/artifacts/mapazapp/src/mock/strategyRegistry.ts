/**
 * Checkpoint 7 — mock strategy / parameter-set registry (shared with `@workspace/mapazapp-core` fixture).
 * Not broker truth; not real MT5 approval.
 */
export {
  createCheckpoint7MockParameterSetRegistry,
  CHECKPOINT7_MOCK_STRATEGY_ID,
} from "@workspace/mapazapp-core";
